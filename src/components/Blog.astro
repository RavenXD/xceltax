---
const posts = [
  {
    category: 'Entity strategy',
    title: 'The 2025 guide to S-Corp elections for solo entrepreneurs',
    description:
      'Understand when an S-Corp election makes sense, how reasonable compensation works, and the exact timeline to transition.',
    cta: 'Read article',
    href: '/blog/s-corp-election-guide'
  },
  {
    category: 'Bookkeeping',
    title: 'Monthly close checklist for service-based businesses',
    description:
      'A repeatable month-end workflow to reconcile accounts, categorize expenses, and stay audit-ready without the headache.',
    cta: 'Download checklist',
    href: '/blog/monthly-close-checklist'
  },
  {
    category: 'Tax planning',
    title: 'How to forecast quarterly taxes with confidence',
    description:
      'Learn a focused framework to calculate estimated payments, avoid penalties, and forecast cash flow through busy seasons.',
    cta: 'View framework',
    href: '/blog/quarterly-tax-forecast'
  }
];
---
<section
  id="blog"
  class="relative overflow-hidden bg-white py-24"
>
  <div class="pointer-events-none absolute inset-x-0 top-0 h-32 bg-gradient-to-b from-primary/10 to-transparent"></div>
  <div class="relative mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="mx-auto max-w-3xl text-center">
      <span class="inline-flex items-center rounded-full border border-primary/20 bg-primary/10 px-4 py-1 text-xs font-semibold uppercase tracking-[0.35em] text-primary">
        Resources
      </span>
      <h2 class="mt-6 text-4xl font-semibold text-slate-900 sm:text-5xl">Fresh guidance from the finance pod</h2>
      <p class="mt-4 text-lg text-slate-600">
        Practical guides and expert insights to help you make confident decisions with taxes, payroll, and cash planning.
      </p>
    </div>

    <div class="mt-16 grid gap-8 md:grid-cols-2 lg:grid-cols-3">
      {posts.map((post) => (
        <article
          class="group relative flex h-full flex-col overflow-hidden rounded-[2rem] border border-white/70 bg-white/80 p-8 shadow-[0_45px_90px_-65px_rgba(79,61,255,0.55)] transition-transform duration-300 hover:-translate-y-2"
        >
          <div class="absolute inset-0 bg-gradient-to-br from-primary/10 via-transparent to-accent/10 opacity-0 transition-opacity duration-300 group-hover:opacity-100"></div>
          <div class="relative flex flex-1 flex-col">
            <p class="text-xs font-semibold uppercase tracking-[0.35em] text-primary">{post.category}</p>
            <h3 class="mt-4 text-2xl font-semibold text-slate-900">{post.title}</h3>
            <p class="mt-4 text-base leading-relaxed text-slate-600">{post.description}</p>
            <a
              href={post.href}
              class="mt-8 inline-flex items-center gap-2 text-sm font-semibold text-primary transition-colors duration-200 group-hover:text-accent"
            >
              {post.cta}
              <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
            </a>
          </div>
        </article>
      ))}
    </div>
  </div>
</section>
