<section class="relative overflow-hidden bg-[#0a112c] py-24 text-slate-50">
  <!-- background glows -->
  <div class="pointer-events-none absolute -left-24 -top-24 h-[680px] w-[680px] rounded-full bg-primary/30 blur-3xl"></div>
  <div class="pointer-events-none absolute -right-24 top-32 h-[540px] w-[540px] rounded-full bg-accent/30 blur-3xl"></div>
  <div class="pointer-events-none absolute inset-x-0 bottom-[-30%] h-72 bg-gradient-to-t from-[#0a112c] via-transparent to-transparent"></div>

  <!-- subtle grid pattern -->
  <svg aria-hidden="true" class="pointer-events-none absolute inset-0 h-full w-full opacity-[0.08]">
    <defs>
      <pattern id="grid" width="32" height="32" patternUnits="userSpaceOnUse">
        <path d="M32 0H0V32" fill="none" stroke="currentColor" stroke-width="0.6" />
      </pattern>
    </defs>
    <rect width="100%" height="100%" fill="url(#grid)" class="text-slate-200" />
  </svg>

  <div class="relative mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="grid gap-16 lg:grid-cols-[1.02fr_0.98fr] lg:items-center">

      <!-- LEFT -->
      <div class="max-w-xl">
        <p class="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-3 py-1 text-[11px] font-semibold uppercase tracking-[0.22em] text-white/70">
          <span class="inline-block h-1.5 w-1.5 rounded-full bg-accent"></span>
          For founders & independents
        </p>

        <h1 class="mt-5 text-4xl font-semibold leading-[1.1] tracking-tight text-white sm:text-5xl">
          Your entire back office, synchronized — tax, bookkeeping, and payroll handled by experts.
        </h1>

        <p class="mt-5 text-lg text-slate-300/95 sm:text-xl">
          XcelTax keeps you compliant, proactive, and growth-ready with a dedicated CFO squad that understands independent entrepreneurs.
        </p>

        <div class="mt-8 flex flex-wrap gap-3 text-sm text-slate-200/85">
          <span class="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-4 py-2 backdrop-blur">
            <span class="h-1.5 w-1.5 rounded-full bg-accent"></span> Predictable quarterly estimates
          </span>
          <span class="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-4 py-2 backdrop-blur">
            <span class="h-1.5 w-1.5 rounded-full bg-accent"></span> Scenario planning dashboards
          </span>
          <span class="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-4 py-2 backdrop-blur">
            <span class="h-1.5 w-1.5 rounded-full bg-accent"></span> Real-time compliance alerts
          </span>
        </div>

        <div class="mt-10 flex flex-col gap-3 sm:flex-row sm:items-center">
          <a
            href="/contact"
            class="inline-flex items-center justify-center rounded-full bg-gradient-to-r from-primary to-accent px-8 py-3 text-base font-semibold text-slate-900 shadow-[0_20px_35px_-20px_rgba(255,137,166,0.9)] transition-transform duration-200 hover:-translate-y-1"
          >
            Schedule a strategy session
          </a>
          <a
            href="#services"
            class="inline-flex items-center justify-center rounded-full border border-white/20 px-8 py-3 text-base font-semibold text-white transition-colors duration-200 hover:border-white/40 hover:bg-white/10"
          >
            See how we operate
          </a>
        </div>

      
      </div>

      <!-- RIGHT -->
      <div class="relative">
        <div class="pointer-events-none absolute -left-12 top-16 hidden h-72 w-72 rounded-full bg-primary/35 blur-3xl lg:block"></div>
        <div class="pointer-events-none absolute -right-10 bottom-10 hidden h-44 w-44 rounded-full bg-accent/35 blur-3xl lg:block"></div>

        <div class="relative mx-auto max-w-xl">
          <!-- main dashboard -->
          <div class="relative overflow-hidden rounded-[2rem] border border-white/15 bg-white/5 p-6 shadow-[0_30px_70px_-35px_rgba(15,15,45,0.85)] backdrop-blur">
            <div class="flex items-center justify-between">
              <div>
                <p class="text-xs font-semibold uppercase tracking-[0.28em] text-white/50">Cash runway</p>
                <p class="mt-1 text-2xl font-semibold text-white">$417,900</p>
              </div>
              <span class="rounded-full border border-emerald-400/30 bg-emerald-400/10 px-3 py-1 text-xs font-semibold text-emerald-300">
                +6.2% MoM
              </span>
            </div>

            <!-- inline chart -->
            <svg viewBox="0 0 560 160" class="mt-6 h-32 w-full">
              <defs>
                <linearGradient id="grad" x1="0" x2="0" y1="0" y2="1">
                  <stop offset="0%" stop-color="rgba(255,255,255,0.6)"></stop>
                  <stop offset="100%" stop-color="rgba(255,255,255,0.05)"></stop>
                </linearGradient>
              </defs>
              <polyline
                fill="url(#grad)"
                stroke="none"
                points="0,120 40,110 80,128 120,95 160,90 200,70 240,80 280,58 320,65 360,48 400,55 440,40 480,50 520,35 560,42 560,160 0,160"
              ></polyline>
              <polyline
                fill="none"
                stroke="rgba(255,255,255,0.8)"
                stroke-width="2.2"
                stroke-linecap="round"
                stroke-linejoin="round"
                points="0,120 40,110 80,128 120,95 160,90 200,70 240,80 280,58 320,65 360,48 400,55 440,40 480,50 520,35 560,42"
              ></polyline>
            </svg>

            <!-- KPIs -->
            <div class="mt-6 grid grid-cols-3 gap-3">
              <div class="rounded-xl border border-white/10 bg-white/5 p-4">
                <p class="text-[11px] uppercase tracking-[0.22em] text-white/60">AR aging</p>
                <p class="mt-1 text-lg font-semibold text-white">$18.4k</p>
                <p class="text-xs text-white/50">≤ 30 days</p>
              </div>
              <div class="rounded-xl border border-white/10 bg-white/5 p-4">
                <p class="text-[11px] uppercase tracking-[0.22em] text-white/60">Payroll</p>
                <p class="mt-1 text-lg font-semibold text-white">$27.1k</p>
                <p class="text-xs text-white/50">next in 5 days</p>
              </div>
              <div class="rounded-xl border border-white/10 bg-white/5 p-4">
                <p class="text-[11px] uppercase tracking-[0.22em] text-white/60">Tax est.</p>
                <p class="mt-1 text-lg font-semibold text-white">$7.8k</p>
                <p class="text-xs text-white/50">Q4 projected</p>
              </div>
            </div>
          </div>

          <!-- floating mini tasks -->
          <div class="absolute -left-6 -top-8 w-[62%]">
            <div class="rounded-2xl border border-white/15 bg-white/10 p-4 backdrop-blur">
              <div class="flex items-center justify-between">
                <p class="text-[11px] uppercase tracking-[0.22em] text-white/70">Tasks</p>
                <span class="rounded-full border border-white/10 bg-white/10 px-2 py-0.5 text-[10px] text-white/80">3 open</span>
              </div>
              <ul class="mt-3 space-y-2 text-sm">
                <li class="flex items-center gap-2">
                  <span class="h-1.5 w-1.5 rounded-full bg-emerald-400"></span>
                  Reconcile Stripe payouts
                </li>
                <li class="flex items-center gap-2">
                  <span class="h-1.5 w-1.5 rounded-full bg-accent"></span>
                  Review contractor onboarding
                </li>
                <li class="flex items-center gap-2">
                  <span class="h-1.5 w-1.5 rounded-full bg-sky-300"></span>
                  Lock October books
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>
